
import { displayScore } from 'quizibo/helpers/display-score';
import { module, test } from 'qunit';

module('Unit | Helper | display score');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayScore([42]);
  assert.ok(result);
});

