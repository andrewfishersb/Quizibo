
import { questionFormat } from 'quizibo/helpers/question-format';
import { module, test } from 'qunit';

module('Unit | Helper | question format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionFormat([42]);
  assert.ok(result);
});

